This file is a legacy reference only.

The real Codex prompt is rendered from `run_newsletter_codex.md.tpl` during install so runtime paths stay platform-specific and do not hardcode a developer machine path.
